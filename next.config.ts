import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Intentionally minimal for Sprint 1 — image domains, redirects, etc.
  // get added as those features arrive in later sprints.
};

export default nextConfig;